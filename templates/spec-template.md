# [功能名称] 规格文档

> **使用说明**：复制此模板，将 `[占位符]` 替换为实际内容。AI 填写的部分标注了"AI填写"，人确认后修改或批准。

---

## 需求理解

我理解的需求是：[AI填写——用自己的话复述需求，不要复制粘贴原始描述。重点说清楚"这个功能是给谁用的、解决什么问题、核心交互是什么"]

**关键假设**（如果有不确定的地方，在此列出）：
- [AI填写——例如："假设用户已登录才能使用此功能"]
- [AI填写——例如："假设数据量不超过10万条"]

---

## 验收标准

> 格式："当[条件]时，应该[预期行为]"。每条必须可独立验证。

### 核心功能

1. 当[条件]时，应该[预期行为]
2. 当[条件]时，应该[预期行为]
3. 当[条件]时，应该[预期行为]

### 用户交互

4. 当[用户操作]时，应该[界面反馈]
5. 当[用户操作]时，应该[界面反馈]

### 数据持久化

6. 当[数据操作]时，应该[存储行为]
7. 当[数据操作]时，应该[存储行为]

---

## 边界条件和异常场景

> 列出所有"不正常"的情况以及系统应该如何处理。

### 输入异常

- 当[用户输入为空]时，应该[具体处理方式]
- 当[用户输入超长/超大]时，应该[具体处理方式]
- 当[用户输入包含特殊字符]时，应该[具体处理方式]

### 系统异常

- 当[网络断开/超时]时，应该[具体处理方式]
- 当[依赖服务不可用]时，应该[具体处理方式]
- 当[并发冲突]时，应该[具体处理方式]

### 权限和安全

- 当[未登录用户访问]时，应该[具体处理方式]
- 当[无权限用户操作]时，应该[具体处理方式]

---

## 修改范围

| 文件 | 修改内容 | 新增/修改 | 风险点 |
|------|---------|----------|--------|
| [文件路径] | [做什么改动] | 新增/修改 | [可能影响的其他功能] |
| [文件路径] | [做什么改动] | 新增/修改 | [可能影响的其他功能] |
| [文件路径] | [做什么改动] | 新增/修改 | [可能影响的其他功能] |

**预计修改文件数**：[X] 个
**推荐执行模式**：[Express / Standard / Full]（参考 single-focus-principle.md）

---

## 依赖和约束

### 外部依赖

- [列出依赖的其他模块/服务/第三方API]
- [列出需要的数据库变更]
- [列出需要的环境变量/配置]

### 技术约束

- [列出技术限制，如"不引入新框架"]
- [列出性能要求，如"接口响应<500ms"]
- [列出兼容性要求，如"需兼容旧版API"]

### 不在本次范围内

- [明确列出本次不做的事情，避免范围蔓延]
- [例如："本次不做批量导入，后续迭代"]

---

## 实现顺序建议

> AI 建议的实现顺序，人可以调整。

1. **第一步**：[最基础的模块，其他模块依赖它]
2. **第二步**：[核心业务逻辑]
3. **第三步**：[前端/交互层]
4. **第四步**：[边界条件和错误处理]
5. **第五步**：[集成测试和验证]

---

## 确认清单

- [ ] 需求理解准确
- [ ] 验收标准完整且可验证
- [ ] 边界条件已覆盖主要场景
- [ ] 修改范围合理，无遗漏
- [ ] 依赖和约束已明确
- [ ] 不在范围内的事项已声明

**确认人**：[姓名]
**确认日期**：[YYYY-MM-DD]
**确认结果**：[ ] 通过，开始实现 / [ ] 需要调整（见备注）

**备注**：[如有调整，在此说明]
